Frontend (React)

- Minimal file-upload UI at `src/App.js`.
- To run locally without Docker: `npm install` then `npm start` in `frontend/`.
- The UI posts to `/api/upload` (same-origin proxy expected when developing locally).
